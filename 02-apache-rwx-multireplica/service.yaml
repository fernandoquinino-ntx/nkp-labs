# A stable in-cluster address for the apache pods (load-balanced across the replicas).
apiVersion: v1
kind: Service
metadata:
  name: apache
  namespace: ${NAMESPACE}
  labels: { app: apache }
spec:
  type: ClusterIP
  selector: { app: apache }
  ports:
    - name: http
      port: 80
      targetPort: 80
